apiVersion: v1
kind: Service
metadata:
  name: {{ include "performance-testing.fullname" . }}
  labels:
    {{- include "performance-testing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "performance-testing.selectorLabels" . | nindent 4 }}
