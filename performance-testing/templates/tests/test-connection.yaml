apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "performance-testing.fullname" . }}-test-connection"
  labels:
    {{- include "performance-testing.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "performance-testing.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
